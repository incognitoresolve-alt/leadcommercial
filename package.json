{
  "name": "leadcommercial",
  "version": "1.0.0",
  "description": "Outil de generation de leads - simulateur Mon Ecart Pension + capture de leads (ECART / KIT)",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --env-file-if-exists=.env server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "express": "^4.21.0"
  }
}
